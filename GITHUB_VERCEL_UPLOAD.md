# GitHub / Vercel upload

The application HTML is unchanged byte-for-byte from the supplied `profex-dashboard.html`.

## GitHub
Upload all files in this folder to the root of your repository. `index.html` is included as an exact copy of the app HTML for static hosting.

## Vercel
Import the GitHub repository in Vercel. No build command is required for this static project.
